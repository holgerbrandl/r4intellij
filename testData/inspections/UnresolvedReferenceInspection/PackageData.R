# since it is asscoiated with base the package should be resolvable
iris


## try non-base data before loading package
<warning descr="Unresolved reference">diamonds</warning>

## load the package and try again
require(dplyr)

diamonds